# ==============================================================================
# ORB Mining Bot Configuration
# Smart Autonomous Bot - Threshold-Based Operation
# ==============================================================================

# ------------------------------------------------------------------------------
# Wallet & Network
# ------------------------------------------------------------------------------
PRIVATE_KEY=your_base58_private_key_here
RPC_ENDPOINT=https://api.mainnet-beta.solana.com
ORB_PROGRAM_ID=boreXQWsKpsJz5RR9BMtN8Vk4ndAk23sutj8spWYhwk
ORB_TOKEN_MINT=orebyr4mDiPDVgnfqvF5xiu5gKnh94Szuz8dqgNqdJn
NETWORK=mainnet-beta

# ------------------------------------------------------------------------------
# Mining Thresholds
# ------------------------------------------------------------------------------
# Only mine when global motherload >= this value (profitability check)
# Conservative strategy:
#   200-399 ORB: Mine conservatively (100 rounds, low SOL per round)
#   400-699 ORB: Start getting aggressive (reduce rounds, more SOL per round)
#   700+ ORB: Maximum aggression (30 rounds minimum, high SOL per round)
MOTHERLOAD_THRESHOLD=200

# How often to check for new rounds (milliseconds)
CHECK_ROUND_INTERVAL_MS=10000

# ------------------------------------------------------------------------------
# Automation Account Setup (First Run Only)
# ------------------------------------------------------------------------------
# What % of wallet SOL to use for initial automation funding
INITIAL_AUTOMATION_BUDGET_PCT=90

# Minimum automation account balance before triggering refund (SOL)
MIN_AUTOMATION_BALANCE=0.5

# ------------------------------------------------------------------------------
# Auto-Claim Thresholds
# ------------------------------------------------------------------------------
# Auto-claim SOL rewards when balance >= this (SOL)
AUTO_CLAIM_SOL_THRESHOLD=0.1

# Auto-claim ORB rewards from mining when balance >= this (ORB)
AUTO_CLAIM_ORB_THRESHOLD=1.0

# Auto-claim ORB rewards from staking when balance >= this (ORB)
AUTO_CLAIM_STAKING_ORB_THRESHOLD=0.5

# How often to check rewards for claiming (milliseconds, 30 seconds default)
CHECK_REWARDS_INTERVAL_MS=30000

# ------------------------------------------------------------------------------
# Auto-Swap Settings (Refund Automation Account)
# ------------------------------------------------------------------------------
# Enable auto-swapping ORB to SOL when automation balance is low
AUTO_SWAP_ENABLED=true

# Amount of ORB to swap each time (DEPRECATED - now swaps all available ORB)
SWAP_ORB_AMOUNT=10

# Never go below this ORB balance (safety reserve)
# Set to 0 to swap ALL ORB, or keep some as reserve
MIN_ORB_TO_KEEP=2

# Slippage tolerance for swaps (basis points, 50 = 0.5%)
SLIPPAGE_BPS=50

# Jupiter API endpoint (lite-api v1 - working endpoint)
JUPITER_API_URL=https://lite-api.jup.ag/swap/v1

# ------------------------------------------------------------------------------
# Auto-Stake Settings (Optional)
# ------------------------------------------------------------------------------
# Enable auto-staking of excess ORB for passive yield
AUTO_STAKE_ENABLED=false

# Auto-stake when ORB balance >= this threshold
STAKE_ORB_THRESHOLD=50

# ------------------------------------------------------------------------------
# Safety Settings
# ------------------------------------------------------------------------------
# Dry run mode - simulate without sending real transactions (testing)
DRY_RUN=false

# Minimum wallet SOL balance to maintain (safety reserve)
MIN_SOL_BALANCE=0.1

# Rate limiting between operations (milliseconds)
RATE_LIMIT_MS=1000

# ------------------------------------------------------------------------------
# Legacy Settings (for backward compatibility - can be ignored)
# ------------------------------------------------------------------------------
BOT_ACTION=auto-deploy
DEPLOY_STRATEGY=all
SOL_PER_DEPLOYMENT=0.01
MIN_SOL_FOR_DEPLOYMENT=0.3
AUTO_CLAIM_ENABLED=true
CLAIM_THRESHOLD_SOL=1.0
CLAIM_THRESHOLD_ORB=100
CLAIM_TYPE=both
CLAIM_FROM_MINING=true
CLAIM_FROM_STAKING=true
AUTO_DEPLOY_ITERATIONS=0
DEPLOY_MAX_RETRIES=3
SMART_ROUND_MANAGEMENT=true
PAUSE_IF_LOW_SOL=true
RESUME_NEXT_ROUND=true
ENABLE_JUPITER_SWAP=true
AUTO_SWAP_WHEN_LOW_SOL=true
REQUIRE_CONFIRMATION=false
